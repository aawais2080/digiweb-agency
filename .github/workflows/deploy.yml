name: 🚀 Build and Deploy to Hostinger

on:
  push:
    branches:
      - main  # Trigger only when you push to the main branch

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4

    - name: 🔨 Build Project
      env:
        VITE_WEB3FORMS_KEY: ${{ secrets.WEB3FORMS_ACCESS_KEY }}
      run: |
        npm install
        npm run build

    - name: 📂 Sync files to Hostinger
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        # Most React apps build into a 'dist' folder (Vite) or 'build' folder (CRA)
        local-dir: ./dist/public/ 
        server-dir: /
        timeout: 180000  # Sets timeout to 180 seconds